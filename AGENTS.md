# AI Agent Instructions

Before editing this project, read `documentation/AI_AGENT_RULES.md`.

For design-only tasks, do not touch backend, admin, Supabase, API, data-layer, authentication, cron publish, AI model, prompt template, import, comments/contact, or core business logic files.

Ask before modifying core logic or any file under:

- `supabase/`
- `src/lib/`
- `src/pages/api/`
- `src/pages/admin/`
- `src/layouts/AdminLayout.astro`

Always run:

```bash
npm run build
```

In the final response, report exactly which files changed and whether the build passed.

